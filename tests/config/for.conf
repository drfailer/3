title = "for"
category = "statements"
description = "Testing the syntax of `for` statement."

# files
dir = "stmts"
src = "for.3"

# result
exit_code = 0
should_compile = true
should_run = true

# compiler
flags = []
ldflags = []
platforms = [ "x86_64" ]
