title = "cnd"
category = "syntax"
description = "Testing the syntax of `cnd` statement."

# files
dir = "syntax/cnd"
src = "cnd.3"

# result
exit_code = 0
should_compile = true
should_run = true

# compiler
flags = [ ]
ldflags = []
platforms = [ "x86_64" ]
