# recursive includes
include ../test-same.prog;

fn fonc(int a) -> int {
  flt b;
  mov(b, mul(1.3, a));
  return b; # avertissement: type de retour
}

fn test_sub() {
  chr c;

  print("test_sub\n");
  mov(c, fonc(4, 3)); # type error
}
