int foo() bgn
    flt f
    mov(f, 3.14)
    ret f ~~~ warning: implicit conversion on the return type
end

nil bar(int a, flt b, chr c) bgn
    shw("hello\n")
    ret 0 ~~~ error: invalid return
end

int baz(int a, flt b, chr c) bgn
    shw("hello\n")
    ret nil ~~~ error: invalid return
end

int fizz() bgn
    shw("hello\n")
    ~~~ error: no return in a non nil function (FIXME: not working yet)
end


int main() bgn
    chr c
    mov(c, foo())     ~~~ warning: implicit conversion
    foo(1, 2)         ~~~ error: too many arguments to foo
    bar(1, 3.14, 'a') ~~~ should compile
    bar('a', 1, 3.14) ~~~ warnings (FIXME: not working)
    bar()             ~~~ error: to few arguments
    ret 0
end
